formulas:
  NumeroSessioni: |
    sum(file.hasProperty(Sessione))
views:
  - type: table
    name: Table
    filters:
      and:
        - Gioco == link("Daggerheart")
        - Sessione == "0"
    order:
      - file.name
